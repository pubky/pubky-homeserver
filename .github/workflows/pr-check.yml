name: PR Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up MSRV toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
      - name: Cache
        uses: Swatinem/rust-cache@v2
      - name: Verify MSRV
        run: cargo check --workspace --all-features

  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
          components: clippy
      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: Lint (host targets)
        run: cargo clippy --workspace --all-features --exclude pubky-wasm -- -D warnings

      - name: Add wasm target
        run: rustup target add wasm32-unknown-unknown

      - name: Lint wasm crate (wasm32)
        run: cargo clippy -p pubky-wasm --target wasm32-unknown-unknown -- -D warnings

  helper-crates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
          components: rustfmt, clippy
      - name: Cache
        uses: Swatinem/rust-cache@v2
      - name: Check excluded helper crates
        run: |
          set -e
          for manifest in \
            test_utils/drop_db_helper/Cargo.toml \
            test_utils/test_macro/Cargo.toml \
            test_utils/pubky_test/Cargo.toml
          do
            cargo fmt --check --manifest-path "$manifest"
            cargo clippy --manifest-path "$manifest" --all-targets -- -D warnings
            cargo test --manifest-path "$manifest"
          done

  test:
    services:
      postgres:
        image: postgres:18-alpine          # official image
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: postgres
        ports: ['127.0.0.1:5432:5432']      # optional – makes it reachable on localhost
        options: >-                         # give the container a health check
          --health-cmd="pg_isready -U test_user"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=5

    env:
      # Your code can read these, or just use the defaults above
      TEST_PUBKY_CONNECTION_STRING: postgres://test_user:test_pass@localhost:5432/postgres?pubky-test=true

    strategy:
      matrix:
        crate:
          [
            pubky,
            pubky-common,
            pubky-homeserver,
            pubky-testnet,
            e2e,
          ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
      - name: Install Nextest
        uses: taiki-e/install-action@nextest
      - name: Cache
        uses: Swatinem/rust-cache@v2
      - name: Wait for PostgreSQL
        run: |
          for i in {1..10}; do
            pg_isready -h localhost -p 5432 -U test_user && break
            echo "Postgres not ready yet… sleeping"
            sleep 3
          done
      - name: Run doctests and nextest
        run: |
          set -e
          # nextest does not support doctests yet.
          cargo test --doc -p ${{ matrix.crate }} --all-features

          if cargo nextest run \
            -p ${{ matrix.crate }} \
            --all-features \
            --test-threads num-cpus \
            --retries 2 \
            --no-fail-fast \
            --verbose; then
            echo "Tests passed for ${{ matrix.crate }}"
          else
            if [ $? -eq 4 ]; then
              echo "No tests found for ${{ matrix.crate }}"
              exit 0
            else
              echo "Tests failed for ${{ matrix.crate }}"
              exit 1
            fi
          fi

  doc:
    strategy:
      matrix:
        crate: [pubky, pubky-testnet]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
      - name: Cache
        uses: Swatinem/rust-cache@v2
      - name: Build docs
        run: cargo doc -p ${{ matrix.crate }} --all-features --no-deps

  examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
      - name: Cache
        uses: Swatinem/rust-cache@v2
      - name: Cargo check examples
        run: |
          cargo check -p pubky-core-examples --all-features --bins

  wasm-test:
    services:
      postgres:
        image: postgres:18-alpine          # official image
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: postgres
        ports: ['127.0.0.1:5433:5432']      # optional – makes it reachable on localhost
        options: >-                         # give the container a health check
          --health-cmd="pg_isready -U test_user"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=5
    env:
      # Your code can read these, or just use the defaults above
      TEST_PUBKY_CONNECTION_STRING: postgres://test_user:test_pass@localhost:5433/postgres?pubky-test=true

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
          target: wasm32-unknown-unknown

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: pubky-sdk/bindings/js/pkg/package-lock.json

      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack

      - name: Install JS dependencies
        working-directory: pubky-sdk/bindings/js/pkg
        run: npm ci --prefer-offline --no-audit --no-fund

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: Run wasm-pack unit tests
        working-directory: pubky-sdk/bindings/js
        run: wasm-pack test --node

      - name: Install Electron/X deps
        run: |
          set -eux
          sudo apt-get update
          # base deps (don’t include libasound here so a single missing package doesn’t abort)
          sudo apt-get install -y --no-install-recommends \
            xvfb \
            libgtk-3-0 \
            libnss3 \
            libxss1 \
            libx11-xcb1 \
            libxcb-dri3-0 \
            libdrm2 \
            libgbm1 \
            libxdamage1 \
            libxshmfence1 \
            libxtst6
          # asound: Ubuntu 24.04 uses libasound2t64; older images still use libasound2
          sudo apt-get install -y --no-install-recommends libasound2t64 || \
          sudo apt-get install -y --no-install-recommends libasound2

      - name: Sanity Check (Electron works)
        run: xvfb-run -a npx electron --no-sandbox --version

      - name: Build WASM
        working-directory: pubky-sdk/bindings/js/pkg
        run: |
          npm run build

      - name: Run the testnet
        working-directory: pubky-sdk/bindings/js/pkg
        run: npm run testnet > testnet.log 2>&1 &

      - name: Wait for testnet homeserver and HTTP relay
        run: |
          timeout=180
          count=0
          until nc -zv 127.0.0.1 6286; do
            if [ $count -ge $timeout ]; then
              echo "Timeout: Testnet homeserver did not start within 3 minutes."
              exit 1
            fi
            echo "Waiting for testnet homeserver to be ready... ($count/$timeout)"
            sleep 1
            count=$((count + 1))
          done
          echo "Testnet homeserver is ready."

          count=0
          until nc -zv 127.0.0.1 15412; do
            if [ $count -ge $timeout ]; then
              echo "Timeout: HTTP relay did not start within 3 minutes."
              exit 1
            fi
            echo "Waiting for HTTP relay to be ready... ($count/$timeout)"
            sleep 1
            count=$((count + 1))
          done
          echo "HTTP relay is ready."

      - name: Node tests
        working-directory: pubky-sdk/bindings/js/pkg
        run: npm run test-nodejs

      - name: Browser tests (Electron under Xvfb)
        working-directory: pubky-sdk/bindings/js/pkg
        env:
          ELECTRON_DISABLE_SANDBOX: "1" # CI-safe
        run: |
          npx electron --version || true
          xvfb-run -a npm run test-browser:ci

      - name: Show testnet logs if tests fail
        working-directory: pubky-sdk/bindings/js/pkg
        if: failure()
        run: cat testnet.log

  docker-postgres-test:
    # This job tests the docker-postgres feature WITHOUT an external postgres service
    # to verify that the Docker-managed postgres works as a standalone solution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89"
      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
      - name: Run docker postgres tests
        run: |
          cargo test -p pubky-testnet \
            --features docker-postgres \
            docker_postgres \
            -- --test-threads=1

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build .
